on: [push]
jobs:
  Build-and-Deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: uses:actions/setup-node@v3
        with:
          node-version: 18
      - run: npm ci
      - run: npm test

        
